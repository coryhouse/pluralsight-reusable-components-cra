// Place variables used in multiple styles up here
const errorColor = 'red';

// Export default theme here. Use centralized variables above as needed.
const defaultTheme = {
  error: {
    color: errorColor,
    border: 'solid 1px red'
  },
  textColor: 'black',
  fieldset: {
    marginBottom: 16
  }
}

export default defaultTheme;
